# https://pre-commit.com — install: pip install pre-commit && pre-commit install
repos:
  - repo: local
    hooks:
      - id: sync-epic-docs-check
        name: sprint-status vs epics.md (sync_epic_docs)
        entry: python3 scripts/sync_epic_docs.py check
        language: system
        pass_filenames: false
