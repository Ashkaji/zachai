# Retrospective: Epic 13 — L6: Robustesse Collaboration, Performance Bible & Clarté API

**Date:** 2026-04-13  
**Status:** Complete  
**Project:** zachai

---

## English

### Epic summary

Epic 13 closed gaps called out in Epic 11–12 retros and reviews: **explicit restore failure semantics** for all collaborators (FR21 complement), **optional Redis caching** for hot Bible verse reads (FR26 / NFR3–9), and **developer experience** via **OpenAPI-aligned** `docs/api-mapping.md` (gateway version stamp, domain sections, deprecated-route table).

| Story | Focus |
|--------|--------|
| **13.1** | `document_restore_failed` → `zachai:document_restore_failed`; ordering before `document_unlocked`; editor error UX; tests extending `test_story_12_3.py` |
| **13.2** | `BIBLE_VERSE_CACHE_*` env flags, generation bump on ingest, graceful Redis fallback, `test_main.py` Bible cache coverage |
| **13.3** | `OPENAPI_TAGS` parity, legend + §1–§17 structure, gateway version line, “routes non implémentées” hygiene |

**Delivery:** 3/3 stories **done** (per `sprint-status.yaml` at time of retro).

---

### What went well

- **Epic 12 retro follow-through:** The deferred **restore-failure broadcast** (12.3 review) became a **first-class contract** (payload `schema_version`, `code`, dedup notes for initiator vs remote).
- **Reuse of proven patterns:** Verse caching **mirrors** grammar cache behavior (TTL, warn-on-failure, no blocking on Redis errors).
- **Single source of truth for API UX:** Tag order in code + mapping doc reduces **integrator confusion** and gives a **checklist** for future route changes.
- **Clear independence:** 13.1 / 13.2 / 13.3 could ship without blocking each other, which kept risk localized.

---

### Challenges & system-level insights

- **Restore path complexity:** Failure handling spans **FastAPI `finally`**, **Redis pub**, **Hocuspocus**, and **React** — correct ordering matters; **Story 13.1 review** still lists open items (e.g. `BaseException` breadth, concurrent publishes, stale failure alert edge cases).
- **Documentation vs code drift:** Epic 13.3 establishes **OPENAPI_TAGS + legend** as the guardrail; **ongoing discipline** is required on every gateway release (version bump in both places).
- **Shield task for Bible cache:** Feature flag + logging are in place; **production validation** of real **p95** remains an **environment** concern, not something unit tests alone prove (explicitly called out in 13.2 AC).

---

### Patterns from story records

| Theme | Evidence |
|--------|-----------|
| **Cross-stack signals** | Redis `hocuspocus:signals` extended with a new `type`; TS forwards distinct stateless payload |
| **Optional Redis** | Bible verse cache defaults **off**; degrades to PostgreSQL |
| **DX / governance** | `docs/api-mapping.md` ↔ `OPENAPI_TAGS` ↔ `FastAPI(..., version=...)` |
| **Review debt** | 13.1 review checklist partially **unchecked** — track as follow-up patches or backlog |

---

### Epic 12 retrospective follow-through

| Epic 12 action | Status for Epic 13 |
|----------------|---------------------|
| Implement **`document_restore_failed`** (or equivalent) with agreed UX | **Done** — Story **13.1** |
| Optional **Bible verse Redis cache** with trigger/metrics mindset | **Done** — Story **13.2** (flag + structured logging hooks) |
| Epic 12 tracking / runbooks | **N/A** to Epic 13 scope |

---

### Next epic / roadmap

**Epic 14** is **not** defined in `.bmad-outputs/planning-artifacts/epics.md` (document ends after the FR coverage map). When the next slice is planned, pull forward: **13.1 review patches**, **OPENAPI/doc parity** on each release, and **staging/load** validation for Bible cache **p95** if the flag is enabled.

**Significant plan rewrite:** **No** — Epic 13 outcomes match the epic definition (restore semantics, optional cache, API clarity).

---

### Action items (SMART)

| # | Action | Owner | Success criteria |
|---|--------|--------|-------------------|
| A1 | Triage **Story 13.1 code-review findings** (exception handling, `finally` publish safety, frontend stale-alert edge case, i18n) into **tracked issues** or a **single hardening story** | **Dev + SM** | Each finding is **closed**, **deferred with rationale**, or **scoped** with AC |
| A2 | Add **release checklist** item: bump **`FastAPI` version** and **`docs/api-mapping.md`** version line together | **Dev / Tech writer** | Documented in team process; no drift in next release PR |
| A3 | Before enabling **Bible verse cache** in production, run **staging metrics** (latency / hit rate) against **NFR** expectations | **Dev + Ops** | Decision recorded (on/off) with numbers |
| A4 | Mark **Epic 13** **done** in sprint tracking after retro | **SM** | `epic-13: done`, `epic-13-retrospective: done` |

---

### Preparation for the next epic (when defined)

- [ ] Confirm **no open P0** from 13.1 review on restore path if the next epic touches collaboration or snapshots.
- [ ] Keep **`OPENAPI_TAGS`** as the **first edit** when adding routes.
- [ ] Re-read **`deferred-work.md`** for 13.1 items if scheduling hardening work.

---

### Readiness (story-complete basis)

- **Functional:** Epic 13 stories delivered per acceptance themes in story files and planning artifact.
- **Quality:** Automated tests extended for restore failure (13.1) and Bible cache (13.2); 13.3 is documentation with manual verification steps.
- **Production:** Stakeholder sign-off, deployment, and **cache** validation remain **project** responsibilities outside this document.

---

### Facilitation snapshot (party mode)

Bob (Scrum Master): "Epic 13 turned two retro promises from Epic 12 into shipped behavior — failed restore is visible everywhere, and verse reads can scale with Redis when you're ready."

Alice (Product Owner): "Integrators get a map that matches Swagger; that saves support time."

Charlie (Senior Dev): "I'll sleep better when we burn down the 13.1 review checklist — the behavior is right, but the edge-case list is real."

Dana (QA Engineer): "Cache defaults off; when someone flips it on, I want staging numbers before we call it production-ready."

---

## Français (traduction)

### Synthèse

L’**Epic 13** livre la **sémantique d’échec de restauration** multi-clients, un **cache Redis optionnel** pour les versets bibliques, et l’**alignement** de `docs/api-mapping.md` sur l’OpenAPI du gateway. **3/3 stories** sont **terminées**.

### Points positifs

- Suite donnée aux actions de la rétro Epic 12 (signal d’échec, cache optionnel).
- Patterns réutilisés (cache type grammaire, contrat de signaux documenté).

### Points d’attention

- Revue **13.1** : plusieurs points de revue encore ouverts — à traiter ou à prioriser explicitement.
- **p95** du cache Bible : validation en environnement réel recommandée avant production.

### Suite

- **Epic 14** : non défini dans `epics.md` — planifier la prochaine incrémentation quand la feuille de route est claire.
- Actions : voir le tableau **Action items** ci-dessus.

---

*Generated per BMad retrospective workflow. Psychological safety: focus on systems and learning, not individual blame.*
