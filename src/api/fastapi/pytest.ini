[pytest]
asyncio_mode = auto
markers =
    api_health: presigned upload / health / auth slice
    api_natures: natures CRUD slice
    api_projects: projects + audio register slice
    api_assignments: assignment dashboard slice
    api_golden: golden set / Camunda slice
    api_transcription: transcription lifecycle slice
    api_export: export / media slice
    api_editor: editor ticket / snapshot / grammar slice
    api_bible: bible engine + cache slice
